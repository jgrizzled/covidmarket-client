import React, { useState } from 'react';
import styled from 'styled-components';

/*  props:
    axis1Data - array of chartDatas 
      chartData: array of {x, y, tickLabelX, tickLabelY}, .color
    axis2Data - ...
    

*/
export default function ZoomSelectChart(props) {
  const [zoom, setZoom] = useState(null);
}
